<h1>User info</h1>

  <%= attachment_image_tag @user, :profile_image, :fill, 60, 60, fallback: "no_image.jpg"%>
  <h4>name</h4>
  <%= @user.name %>
  <h4>introduction</h4>
  <%= @user.introduction %>
  <%= link_to "edit",edit_user_path(@user) %>

<h1>New　Book</h1>
<%= form_with model: @book, local:true do |f| %>
  <h2>title</h2>
  <%= f.text_field :title %>
  <h4>opinion</h4>
  <%= f.text_area :body %>
  <%= f.submit 'Create Book' %>
  <% end %>

<h2>Books</h2>
<table>
<thead>
<tr>
<th>title</th>
<th>opinion</th>
<th colspan="3"></th>
</tr>
</thead>
<tbody>
  <% @books.each do |book| %>
  <tr>
    <td><%= link_to @user do %>
      <%= attachment_image_tag book.user, :profile_image,:fill,40,40, fallback:"no_image.jpeg" %></td>
      <%= @user.name %>
      <% end %>
    <td><%= link_to book_path(book.id) do %>
        <%= book.title %><% end %></td>
    <td><%= book.body %></td>
</tr>
<% end %>
</tbody
</html>